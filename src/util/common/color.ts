import {COLORS} from 'util/palette';

export const DEFAULT = {
  primary: COLORS.WHITE,
  primaryText: COLORS.TRISTESSE,
  buttonTextColor: COLORS.WHITE,
};

export const DARK_MODE = {
  primary: COLORS.BLACK,
  primaryText: COLORS.WHITE,
  buttonTextColor: COLORS.WHITE,
};
